import { SearchBar } from "../search-bar";

export default function SearchBarExample() {
  return (
    <div className="p-4 w-full max-w-md">
      <SearchBar placeholder="Search leads, contacts, companies..." />
    </div>
  );
}
