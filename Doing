Date: 24 Jan 2025
Day: Friday

Today Learning:

1. Revision of Last Work
2. Understand more about Events.
3. Learn more about Form and their controls

Work done:
Date: 23 Jan 2025
Day: Thursday

Today, I am working on below list of task:

1. Searching Methods learn, inside this i learn about
   - like getElementById, getElementsByTagname, getElementsByName, getElementsByClassName
   - querySelector & querySelectorAll
   - elem.closest(css)
   - elem.Matches(css)
   - elem.contains(elem2)
2. Node Properties
   - nodeType, nodeName & tagName
   - innerHTML & outerHTML
   - node value/data
   - textContent
   - hidden property
   - other properties like value, type, href, id
3. Attributes & Properties
   - dom Properties which is inside DOM Object
   - HTML Attributes which is written in HTML
   - learn Attributes methods like hasAttribute, getAttribute, setAttribute, removeAttribute
   - Non-standard Attributes, dataset
4. Modify the Document
   - create Element/textNode using createElement() and createTextNode()
   - insertion method like append(), prepend(), before(), after(), replaceWith()
   - insertAdjecentHTML/Text/Element another method to create Element/Text
   - Remove node using remove()
   - clone node using clonNode()
   - DocumentFragment() to cretate wrapper of list
   - also there  are another olb method like appendChild(), insterBefore(), replaceChild(), removeChild() 


