// For...of loop
//      - walk over all values of Obj
//      - not work with Object because Objects are not accessible without properties



let fruits = ["Apple", "Orange", "Plum"];

// iterates over array elements
for (let fruit of fruits) {
  alert( fruit );
}